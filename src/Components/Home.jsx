import React from 'react'
import Minimal from './Minimal';

const Home = (props) => {
    return (
        <div>
            {/* <Sidebar data={ props.data.userData}/> */}
            {/* <Navbar /> */}
            <Minimal />
        </div>
    )
}

export default Home
